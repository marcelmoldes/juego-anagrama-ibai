<div
  id="chat-container"
  class="max-w-md mx-auto mt-10 bg-gray-100 rounded-xl shadow-lg flex flex-col h-[600px] overflow-hidden"
>
  <!-- HEADER -->
  <div class="flex items-center bg-green-600 text-white px-4 py-3">
    <img
      src="../../src/assets/ibai.jpg"
      alt="Foto de contacto"
      class="w-10 h-10 rounded-full mr-3"
    />
    <div>
      <h3 class="font-semibold leading-tight">Ibai</h3>
      <p class="text-sm text-green-200">En línea</p>
    </div>
  </div>

  <!-- MENSAJES -->
  <div
    id="mensajes"
    class="flex-1 overflow-y-auto px-4 py-3 space-y-2 bg-gray-50"
  ></div>

  <!-- INPUT -->
  <div
    class="flex items-center gap-2 bg-gray-200 p-3 border-t border-gray-300"
  >
    <input
      id="input-msg"
      type="text"
      placeholder="Escribe un mensaje..."
      class="flex-1 rounded-full px-4 py-2 outline-none bg-white"
    />
    <button
      id="btn-enviar"
      class="bg-green-500 text-white px-4 py-2 rounded-full hover:bg-green-600 transition"
    >
      Enviar
    </button>
  </div>
</div>
